# Base URL of the Módulo 1 backend API (see /backend). Not yet consumed by
# the UI — the prototype still runs entirely on mock data/localStorage. This
# is prepared ahead of time so the future integration only needs this value.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
